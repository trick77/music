name: CI

# Tests run on pull requests (feature branches). Master does not run tests — it
# builds and pushes the image (see release.yaml). No image build happens here.
on:
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend (build + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: backend/go.mod
          cache-dependency-path: backend/go.sum
      - run: go build ./...
      - run: go vet ./...
      # The Makefile lives at the repo root and its targets cd into backend/ themselves,
      # so this step overrides the job's backend working-directory.
      - run: make backend-coverage
        working-directory: .

  ui:
    name: UI (build + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ui
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - run: npm ci
      - run: npm run build
      # Root Makefile; the target cds into ui/ itself (see the backend job).
      - run: make fe-coverage
        working-directory: .

  sidecar:
    name: Align sidecar (test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sidecar/align
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      # The align sidecar's custom logic (word grouping/interpolation, language
      # detection) is unit-tested without the multi-GB ML stack — the tests import
      # only grouping.py/language.py, so pytest + langdetect is all that's needed.
      - run: pip install pytest langdetect==1.0.9
      - run: python -m pytest -q
