# Light, more-frequently-changing deps. Kept in their OWN layer (installed last,
# after the heavy ML stack) so bumping one of these — or adding a small dependency
# like langdetect — re-installs and re-pushes only a few MB, never the multi-GB
# torch/whisperx/demucs layer. See requirements-heavy.txt and the Containerfile.
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
langdetect==1.0.9
